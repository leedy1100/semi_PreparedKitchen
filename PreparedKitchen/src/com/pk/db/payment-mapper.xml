<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="paymentmapper">

	<select id="selectList" resultType="paymentDto">
		SELECT PAYMENT_NO, PAYMENT_GROUPNO, ID, PAYMENT_PRICE, RECIPE_NO, MATERIAL_NO, PAYMENT_DATE, SHIPPING_ADDR
		FROM PAYMENT
		ORDER BY PAYMENT_GROUPNO, PAYMENT_NO
	</select>

	<select id="incrementSeq" resultType="int">
		SELECT PAYMENT_GROUPSEQ.NEXTVAL FROM DUAL
	</select>

	<insert id="insert" parameterType="paymentDto">
		INSERT INTO PAYMENT
		VALUES(PAYMENTSEQ.NEXTVAL, #{payment_groupno}, #{id}, #{payment_price}, #{recipe_no}, #{material_no}, SYSDATE, #{shipping_addr})
	</insert>
	
</mapper>
