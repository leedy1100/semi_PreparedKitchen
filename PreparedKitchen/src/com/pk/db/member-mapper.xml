<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="membermapper">
  
  <select id="selectList" resultType="memberDto">
	SELECT ID, PW, NAME, EMAIL, PHONE, ADDR, ENABLED, BIRTH, REGDATE
	FROM MEMBER
	ORDER BY REGDATE DESC
  </select>
  
  <select id="idchk" parameterType="String" resultType="memberDto">
  	SELECT ID, PW, NAME, EMAIL, PHONE, ADDR, ENABLED, BIRTH, REGDATE
  	FROM MEMBER
  	WHERE ID = #{id}
  </select>
  
  <insert id="signup" parameterType="memberDto">
  	INSERT INTO MEMBER
  	VALUES(#{id}, #{pw}, #{name}, #{email}, #{phone}, #{addr}, 'Y', #{birth}, SYSDATE)
  </insert>
  
</mapper>
