DROP SEQUENCE CARTSEQ;
DROP TABLE CART;

CREATE SEQUENCE CARTSEQ;
CREATE TABLE CART(

	CART_NO NUMBER PRIMARY KEY,
	ID VARCHAR2 NOT NULL,
	RECIPE_NO NUMBER NOT NULL,
	MATERIAL_NO NUMBER NOT NULL,
	
	CONSTRAINT FK_CART_ID FOREIGN KEY(ID)
	REFERENCES MEMBER(ID),
	CONSTRAINT FK_CART_RECIPE FOREIGN KEY(RECIPE_NO)
	REFERENCES RECIPE(RECIPE_NO),
	CONSTRAINT FK_CART_RECIPE FOREIGN KEY(MATERIAL_NO)
	REFERENCES MATERIAL(MATERIAL_NO),
);